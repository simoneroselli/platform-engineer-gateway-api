apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: '{{ .Values appFullname }}'
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: '{{ .Values appFullname }}-api'