apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: '{{ .Values.namespace | default "wordsmith-app" }}'
spec:
  type: ExternalName
  externalName: '{{ .Values.database.host | default "wordsmith-db" }}'