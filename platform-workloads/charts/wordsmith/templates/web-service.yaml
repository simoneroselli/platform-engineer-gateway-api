apiVersion: v1
kind: Service
metadata:
  name: '{{ include "wordsmith.fullname" . }}-web'
  namespace: '{{ .Values.namespace | default "wordsmith-app" }}'
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: '{{ include "wordsmith.fullname" . }}-web'