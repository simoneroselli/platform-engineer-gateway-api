apiVersion: v1
kind: Service
metadata:
  name: '{{ .Values appFullname }}-web'
  namespace: '{{ .Values appFullname }}'
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: '{{ .Values appFullname }}-web'